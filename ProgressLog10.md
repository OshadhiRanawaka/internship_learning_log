# Progress Log 10

## Overview
Continuing the Learning Week during my Internship at ProLab R, I worked through I/O redirection, sorting, file statistics, and text filtering topics from the **NDG Linux Essentials** course.
Looked through documentation and continued working on the project - A Payroll and HR Management System

## Resource
- **Course:** NDG Linux Essentials
- **Project** Payroll and HR Management System 
- **Topics Covered:** Multi-stream redirection, STDIN, `sort`, `wc`, `cut`, `grep`, and basic regular expressions

## Daily Progress Log

**Tuesday - 1.9.2026**
- Learned to redirect **STDOUT and STDERR together** using `&>`, or separately to different files using `>` and `2>`
- Covered **STDIN redirection** using `<`, including practical use cases like the `tr` command and combining input/output redirection in a single command
- Learned the **`sort`** command for arranging file/input lines alphabetically or numerically, including field-based sorting with `-t`, `-k`, `-n`, and `-r`, plus multi-field sort examples
- Looked through the documentation for the project and summarised the requirements of it

**Wednesday - 2.9.2026**
- Learned **`wc`** for counting lines, words, and bytes in files, and how to filter to specific stats with `-l`, `-w`, `-c`
- Covered **`cut`** for extracting columns by delimiter (`-d`, `-f`) or character position (`-c`)
- Learned **`grep`** for filtering file content by pattern, including `--color`, `-c`, `-n`, `-v`, `-i`, and `-w` options
- Started **basic regular expressions** — the period (`.`) for matching any character, and bracket (`[ ]`) expressions for matching character sets/ranges, including negation with `^`
- Using Lovable created the route pages **Attendance**, **Leave** and **Payroll** of the Admin Interface of the project

## Key Takeaways
- Comfortable redirecting multiple streams and using STDIN redirection for commands that don't accept file arguments
- Practical fluency with `sort`, `wc`, and `cut` for organizing and extracting data from files
- Strong foundation in `grep` and basic regex patterns (`.`, `[ ]`, `^`) for text filtering and pattern matching
- Better understanding of the project requirements and features

## Next Steps
- Continue with the remaining of the course
- Practice combining `grep`, `sort`, `cut`, and pipes hands-on for real text-processing tasks
- Move forward with the remaining route pages of the project
